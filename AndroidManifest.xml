<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pribadi.parentcontrol">

    <!-- Izin untuk Internet dan Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Izin untuk menampilkan Overlay (Layar Penuh) di atas aplikasi lain -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    
    <!-- Izin untuk menjalankan Background Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Diperlukan untuk Android 14+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParentControl">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Daftarkan Service untuk HP Anak -->
        <service
            android:name=".LockOverlayService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse" />
            
    </application>
</manifest>